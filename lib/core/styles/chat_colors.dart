import 'dart:ui';

class AppColors {
  // ====================================================
  // Primary colors
  // ====================================================
  static const primaryLight = Color(0xFF7590ff);
  static const primary = Color(0xFF2663FF);
  static const primaryDark = Color(0xFF003acb);
  static final appPrimary_8 = primary.withOpacity(.08);
  // ====================================================
  // Details colors
  // ====================================================
  static const appdDtailsLight = Color(0xFFFF9243);
  static const appDetail = Color(0xFFE5833C);

  // ====================================================
  // secondary colors
  // ====================================================
  static const appSecondaryLight = Color(0xFF64ff64);
  static const appSecondary = Color(0xFF00d72f);
  static const appSecondaryDark = Color(0xFF00a400);

  // ====================================================
  // Alerts
  // ====================================================
  static const appAlertSuccess = Color(0xFF38B449);

  static const appAlertWarning = Color(0xFFFDB81E);

  static const appAlertError = Color(0xFFF44336);

// ====================================================
  // black
  // ====================================================
  static const black = Color(0xFF000000);
  static final appNeutralBlack_64 = black.withOpacity(.64);
  static final appNeutralBlack_40 = black.withOpacity(.40);
  static final appNeutralBlack_16 = black.withOpacity(.16);

  // ====================================================
  // white
  // ====================================================
  static const appNeutralWhite = Color(0xFFFFFFFF);
  static const appNeutralWhiteBackground = Color(0xFFF8F8FA);
}
